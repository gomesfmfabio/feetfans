# Story 3.3: Chat Screen with Message History

**Epic:** 3 - Real-Time Messaging & AI Agent System
**Status:** Ready for Review
**Assigned to:** @dev
**Estimated hours:** 5
**Dependencies:** Story 3.1 ✅ (Messages table, SSE/WebSocket)
**Completed:** 2026-05-05

---

## Acceptance Criteria

- [x] **AC1:** Chat page at /messages/[conversationId]
- [x] **AC2:** Displays message history in chronological order
- [x] **AC3:** Each message shows sender nickname, content, timestamp
- [x] **AC4:** Message input field with Send button (Enter key to send)
- [x] **AC5:** Optimistic UI update when sending message
- [x] **AC6:** Real-time message delivery via SSE/WebSocket
- [x] **AC7:** Auto-scroll to bottom when new message received/sent
- [x] **AC8:** Messages >24h old automatically removed from display
- [x] **AC9:** Trial users see banner: "Upgrade to share links and message real buyers"

---

## Technical Notes

```typescript
// Auto-scroll to bottom
const messagesEndRef = useRef<HTMLDivElement>(null);

useEffect(() => {
  messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' });
}, [messages]);

// Send message
const sendMessage = async (content: string) => {
  const { data } = await supabase
    .from('messages')
    .insert({
      conversation_id: conversationId,
      sender_id: userId,
      content,
      expires_at: new Date(Date.now() + 24 * 60 * 60 * 1000),
    })
    .select()
    .single();

  // Optimistically add to UI
  setMessages([...messages, data]);
};
```

---

## Files Created

- `apps/web/app/messages/[conversationId]/page.tsx` - Chat screen with SSE

---

## Dev Agent Record

### Implementation Summary
Complete chat screen with real-time SSE messaging, optimistic UI updates, auto-scroll, message expiration filtering, and trial user banner.

### Technical Decisions
- SSE connection with auto-reconnect (5s delay)
- Optimistic UI updates on message send
- Filter expired messages client-side every minute
- Auto-scroll on messages change using useRef
- Mark messages as read on load and new message
- Trial banner for upgrade prompts

### Features Implemented
**Real-Time Messaging:** SSE connection, auto-reconnect, optimistic updates
**Message Display:** Chronological order, sender bubbles, timestamps, expiry countdown
**Input:** Send button + Enter key, disabled while sending
**Auto-Scroll:** Smooth scroll to bottom on new messages
**Expiration:** Filter >24h messages every minute
**Trial Banner:** Yellow banner for trial users

### File List
- apps/web/app/messages/[conversationId]/page.tsx

### Change Log
- 2026-05-05: Implemented chat screen with SSE and all features
