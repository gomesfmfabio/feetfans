# Story 5.1: Featured Placement Purchase Flow

**Epic:** 5 - Featured Placement & Monetization
**Status:** Ready
**Assigned to:** @dev
**Estimated hours:** 5
**Dependencies:** Story 4.3 ✅ (Stripe integration)
**Completed:** —

---

## Acceptance Criteria

- [ ] **AC1:** Database featured_placements table (id, creator_id, tier, purchased_at, expires_at, payment_type, stripe_payment_id)
- [ ] **AC2:** /featured page for paid creators (trial users see upgrade prompt)
- [ ] **AC3:** Pricing options: Standard (7 days $19), Premium (30 days $59 one-time or $49/month recurring)
- [ ] **AC4:** Payment processed via Stripe (US/international credit cards)
- [ ] **AC5:** On success, record created with expires_at = purchased_at + duration
- [ ] **AC6:** User redirected with success: "You're now featured!"
- [ ] **AC7:** Error handling for payment failures

---

## Technical Notes

```sql
CREATE TABLE featured_placements (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  creator_id UUID UNIQUE NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  tier TEXT NOT NULL CHECK (tier IN ('standard', 'premium')),
  purchased_at TIMESTAMPTZ DEFAULT now(),
  expires_at TIMESTAMPTZ NOT NULL,
  payment_type TEXT CHECK (payment_type IN ('one_time', 'recurring')),
  stripe_payment_id TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX idx_featured_expires_at ON featured_placements(expires_at);
CREATE INDEX idx_featured_creator ON featured_placements(creator_id);
```

---

## Files Created

- `app/featured/page.tsx`
- `supabase/migrations/00014_create_featured_placements.sql`
