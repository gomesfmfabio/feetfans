# Story 4.2: Feature Gating - Trial vs Paid Access

**Epic:** 4 - Trial & Subscription Management
**Status:** Ready
**Assigned to:** @dev
**Estimated hours:** 4
**Dependencies:** Story 4.1 ✅ (Subscription status), Story 3.3 ✅ (Messaging)
**Completed:** —

---

## Acceptance Criteria

- [ ] **AC1:** Trial users can: upload content, view feed/discovery, message AI agents only
- [ ] **AC2:** Trial users CANNOT message real consumers (UI shows "Upgrade to unlock")
- [ ] **AC3:** Paid users (status = 'paid' or 'free_feetfans') have full access
- [ ] **AC4:** Expired users see upgrade prompt, read-only inbox
- [ ] **AC5:** Middleware enforces feature gating on backend API

---

## Technical Notes

```typescript
// Middleware
export async function checkFeatureAccess(userId: string, feature: string): Promise<boolean> {
  const { data: user } = await supabase
    .from('users')
    .select('subscription_status')
    .eq('id', userId)
    .single();

  if (feature === 'message_real_users') {
    return ['paid', 'free_feetfans'].includes(user.subscription_status);
  }

  return true;
}

// In chat screen
{!isPaidUser && recipientIsRealUser && (
  <div className="bg-yellow-100 p-2 text-sm">
    Upgrade to message real buyers - $9/month
  </div>
)}
```

---

## Files Created

- `lib/middleware/feature-gating.ts`
