# Story 3.7: AI Agent Response to Creator Messages

**Epic:** 3 - Real-Time Messaging & AI Agent System
**Status:** Ready
**Assigned to:** @dev
**Estimated hours:** 5
**Dependencies:** Story 3.6 ✅ (AI message generation)
**Completed:** —

---

## Acceptance Criteria

- [ ] **AC1:** When creator sends message to AI agent, backend detects recipient is AI
- [ ] **AC2:** Backend triggers generateAgentResponse(agentId, conversationId, creatorMessage)
- [ ] **AC3:** Fetches conversation history (last 5 messages) and agent personality
- [ ] **AC4:** Prompt constructed to continue conversation naturally (<75 words)
- [ ] **AC5:** If creator is on trial and shares link, agent responds: "I don't see any link"
- [ ] **AC6:** If creator is paid and shares link, agent makes polite excuse
- [ ] **AC7:** AI agents NEVER complete purchases or share payment details

---

## Technical Notes

```typescript
async function generateAgentResponse(agentId: string, conversationId: string, creatorMessage: string) {
  // Fetch conversation history
  const { data: history } = await supabase
    .from('messages')
    .select('content, sender_id')
    .eq('conversation_id', conversationId)
    .order('created_at', { ascending: false })
    .limit(5);

  // Check if creator shared link
  const hasLink = /https?:\/\//.test(creatorMessage);

  const { data: creator } = await supabase
    .from('users')
    .select('subscription_status')
    .eq('id', creatorId)
    .single();

  let prompt = `${agent.personality_prompt}\n\nConversation history:\n${formatHistory(history)}\n\nCreator said: "${creatorMessage}"\n\nRespond naturally (<75 words).`;

  if (hasLink && creator.subscription_status === 'trial') {
    prompt += ' The creator mentioned a link but you cannot see it. Respond that you don\'t see any link.';
  } else if (hasLink) {
    prompt += ' The creator shared a link. Politely acknowledge but make an excuse not to click it now.';
  }

  // Call Claude API
  const message = await anthropic.messages.create({
    model: 'claude-3-haiku-20240307',
    max_tokens: 150,
    messages: [{ role: 'user', content: prompt }],
  });

  return message.content[0].text;
}
```

---

## Files Created

- `lib/ai-agents/response-generator.ts`
