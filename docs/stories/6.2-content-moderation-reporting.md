# Story 6.2: Content Moderation - User Reporting

**Epic:** 6 - Safety, Compliance & Launch Readiness
**Status:** Ready
**Assigned to:** @dev
**Estimated hours:** 3
**Dependencies:** Story 2.3 ✅ (Feed)
**Completed:** —

---

## Acceptance Criteria

- [ ] **AC1:** Report button on content: "Report this content"
- [ ] **AC2:** Modal with reasons (checkboxes) and optional comment
- [ ] **AC3:** Creates record in content_reports table
- [ ] **AC4:** Admin dashboard lists pending reports
- [ ] **AC5:** Admin actions: delete content, warn creator, dismiss
- [ ] **AC6:** Reporter confirmation: "Thank you. We'll review within 24h"
- [ ] **AC7:** No feedback on outcome (privacy)

---

## Technical Notes

```sql
CREATE TABLE content_reports (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  content_id UUID NOT NULL REFERENCES content(id) ON DELETE CASCADE,
  reporter_id UUID NOT NULL REFERENCES users(id),
  reason TEXT NOT NULL,
  comment TEXT,
  status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'reviewed', 'dismissed')),
  created_at TIMESTAMPTZ DEFAULT now()
);
```

---

## Files Created

- `supabase/migrations/00017_create_content_reports.sql`
- `components/ReportModal.tsx`
